apiVersion: v1
kind: Service
metadata:
  name: green-service
spec:
  type: ClusterIP   # internal only (important)
  selector:
    app: myapp
    version: green
  ports:
  - port: 80
    targetPort: 8080