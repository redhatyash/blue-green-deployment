apiVersion: v1
kind: Service
metadata:
  name: app-service
spec:
  type: LoadBalancer
  selector:
    app: myapp
    version: blue  # ← patch to switch traffic
  ports:
  - port: 80
    targetPort: 8080